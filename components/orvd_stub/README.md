# ORVD Stub

Локальная заглушка ОРВД для ответа на `request_takeoff`.

Запуск:

```bash
python -m components.orvd_stub
```

Docker entrypoint:

```bash
docker build -f components/orvd_stub/docker/Dockerfile -t orvd-stub .
```
